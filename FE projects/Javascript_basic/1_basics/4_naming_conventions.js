/**
 * Naming Convention 1
 * 1) 일반적으로 영어를 사용하며 문자와 숫자를 모두 사용할 수 있다.
 * 2) 특수 기호는 언더스코어와 달러를 사용할 수 있음
 * 3) 숫자로 이름을 시작할 수 없음
 * 4) 키워드는 변수명으로 사용할 수 없음
 */
let codeFactory = 'factory'
var $ive = 'ive'
const _yujin = '유진'
console.log(codeFactory);
console.log($ive);
console.log(_yujin);

/**
 * Naming Convention 2
 * 1) camelCase :띄어쓰기를 기준으로 대문자로 표기
 * 2) snake_case ->파이썬 등
 * 3) PascalCase -> c#같은 마이크로소프트 계열의 언어에서...비추
 */
const anYujin = '안유진'



  